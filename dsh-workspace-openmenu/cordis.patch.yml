# dsh bundle layer: mount the workspace quick-open menu.
# Client adds an "打开为 / Open as" button to the session header utility row
# (left of the session-log button); Host launches pwsh/cmd/Explorer/VSCode at
# the session's workspace root.
- insert:
    - id: workspace-open
      name: dsh-workspace-openmenu
