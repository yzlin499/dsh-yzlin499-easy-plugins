# 统一跨平台换行符：仓库内统一存 LF，检出时按平台转换
* text=auto

# PowerShell 脚本固定 CRLF，shell 脚本固定 LF
*.ps1 text eol=crlf
*.sh   text eol=lf

# 二进制文件不做换行转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
